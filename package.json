{
  "name": "novanotes-android",
  "version": "1.0.0",
  "private": true,
  "description": "NovaNotes Android wrapper built from the existing HTML/CSS/JS frontend.",
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "@capacitor/android": "8.5.0"
  }
}
